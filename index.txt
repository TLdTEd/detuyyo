<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nuestro Santuario ✨</title>
    <!-- Tipografías Acogedoras y Elegantes de Google Fonts -->
    <link rel="preconnect" href="https://googleapis.com">
    <link rel="preconnect" href="https://gstatic.com" crossorigin>
    <link href="https://googleapis.com/css2?family=Instrument+Serif:ital@0;1&family=Plus+Jakarta+Sans:wght@300;400;600&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --bg-cosy: #F7F4EF; /* Arena suave */
            --text-dark: #3D3433; /* Marrón chocolate tenue */
            --text-muted: #7A6F6E;
            --accent-warm: #D4A373; /* Terracota / Oro viejo */
            --box-bg: #FFFFFF;
            --overlay-bg: rgba(61, 52, 51, 0.4);
            --smooth-shadow: 0 10px 40px rgba(61, 52, 51, 0.06);
            --border-soft: 1px solid rgba(61, 52, 51, 0.12);
        }

        * { box-sizing: border-box; margin: 0; padding: 0; }

        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            background-color: var(--bg-cosy);
            color: var(--text-dark);
            min-height: 100vh;
            overflow-x: hidden;
            position: relative;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        /* --- CONTROL DISCRETO DE AMBIENTE --- */
        .ambient-controller {
            position: absolute;
            top: 20px;
            right: 20px;
            background: var(--box-bg);
            border: var(--border-soft);
            padding: 10px 15px;
            border-radius: 30px;
            box-shadow: var(--smooth-shadow);
            display: flex;
            align-items: center;
            gap: 10px;
            z-index: 100;
            font-size: 13px;
        }

        .ambient-controller input[type="range"] {
            width: 80px;
            accent-color: var(--accent-warm);
            cursor: pointer;
        }

        .btn-mute {
            background: none; border: none; cursor: pointer; font-size: 16px;
        }

        /* --- MENÚ PRINCIPAL --- */
        .header-santuario {
            text-align: center;
            margin-top: 60px;
            margin-bottom: 40px;
            max-width: 500px;
            z-index: 10;
        }

        .header-santuario h1 {
            font-family: 'Instrument Serif', serif;
            font-size: 48px;
            font-weight: 400;
            letter-spacing: -0.5px;
            margin-bottom: 8px;
        }

        .header-santuario p {
            font-size: 14px;
            color: var(--text-muted);
            letter-spacing: 0.5px;
        }

        .grid-santuario {
            display: grid;
            grid-template-columns: 1fr;
            gap: 16px;
            width: 100%;
            max-width: 480px;
            z-index: 10;
            margin-bottom: 60px;
        }

        .btn-santuario {
            background: var(--box-bg);
            border: var(--border-soft);
            padding: 22px 24px;
            border-radius: 18px;
            text-align: left;
            cursor: pointer;
            box-shadow: var(--smooth-shadow);
            transition: transform 0.2s cubic-bezier(0.4, 0, 0.2, 1), box-shadow 0.2s;
            display: flex;
            flex-direction: column;
            gap: 4px;
        }

        .btn-santuario:hover {
            transform: translateY(-2px);
            box-shadow: 0 15px 45px rgba(61, 52, 51, 0.1);
        }

        .btn-santuario .btn-title {
            font-size: 16px;
            font-weight: 600;
            color: var(--text-dark);
        }

        .btn-santuario .btn-desc {
            font-size: 13px;
            color: var(--text-muted);
        }

        /* --- AUDIOS DE VOZ FLOTANTES (BUBBLES) --- */
        .voice-bubble {
            position: absolute;
            background: rgba(255, 255, 255, 0.6);
            border: var(--border-soft);
            backdrop-filter: blur(5px);
            -webkit-backdrop-filter: blur(5px);
            width: 45px;
            height: 45px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            box-shadow: var(--smooth-shadow);
            z-index: 5;
            font-size: 16px;
            transition: transform 0.3s ease, background-color 0.3s;
            animation: drift 6s infinite ease-in-out;
        }

        .voice-bubble:hover {
            background: var(--box-bg);
            transform: scale(1.1);
        }

        /* --- VENTANAS EMERGENTES (MODALES) --- */
        .modal-overlay {
            position: fixed;
            top: 0; left: 0; width: 100vw; height: 100vh;
            background: var(--overlay-bg);
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
            z-index: 500;
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            visibility: hidden;
            transition: opacity 0.4s ease, visibility 0.4s;
            padding: 20px;
        }

        .modal-overlay.active { opacity: 1; visibility: visible; }

        .modal-card {
            background: var(--box-bg);
            border: var(--border-soft);
            border-radius: 24px;
            padding: 30px 24px;
            max-width: 440px;
            width: 100%;
            box-shadow: 0 20px 60px rgba(0,0,0,0.15);
            position: relative;
            transform: translateY(20px);
            transition: transform 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
            max-height: 90vh;
            overflow-y: auto;
        }

        .modal-overlay.active .modal-card { transform: translateY(0); }

        .btn-close-modal {
            position: absolute;
            top: 20px; right: 20px;
            background: var(--bg-cosy);
            border: none;
            width: 28px; height: 28px;
            border-radius: 50%;
            cursor: pointer;
            font-size: 12px;
            color: var(--text-dark);
            display: flex; align-items: center; justify-content: center;
        }

        @keyframes drift {
            0%, 100% { transform: translateY(0) translateX(0); }
            50% { transform: translateY(-15px) translateX(8px); }
        }
    </style>
</head>